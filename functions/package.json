{
  "name": "eduformium-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for Eduformium SMS",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "deploy:dev":  "firebase use eduformium-dev  && firebase deploy --only functions",
    "deploy:prod": "firebase use eduformium-sms  && firebase deploy --only functions",
    "deploy:all":  "npm run deploy:dev && npm run deploy:prod"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
